name: 药丸论坛自动签到

on:
  schedule:
    - cron: '30 16 * * *'  # 北京时间每天00:30执行
  workflow_dispatch:  # 手动触发

jobs:
  checkin:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 配置Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'  # 启用pip缓存（依赖requirements.txt）

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # 从依赖文件安装
          sudo apt-get update && sudo apt-get install -y jq

      - name: 打印执行时间
        run: |
          echo "执行时间（北京时间）：$(date -d '+8 hour' '+%Y-%m-%d %H:%M:%S')"

      - name: 执行签到脚本
        id: checkin_step
        env:
          INVITES_COOKIE: ${{ secrets.INVITES_COOKIE }}
          INVITES_USERNAME: ${{ secrets.INVITES_USERNAME }}
          INVITES_PASSWORD: ${{ secrets.INVITES_PASSWORD }}
        run: |
          python checkin.py  # 确保脚本路径正确

      - name: 发送企业微信通知
        if: always()  # 无论成败都发送通知
        env:
          WECHAT_WEBHOOK: ${{ secrets.WECHAT_WEBHOOK }}
          RESULT: ${{ steps.checkin_step.outputs.checkin_result }}
          MSG: ${{ steps.checkin_step.outputs.checkin_msg }}
        run: |
          # 构造标题
          if [ "$RESULT" = "success" ]; then
              TITLE="✅ 药丸论坛签到成功"
          else
              TITLE="❌ 药丸论坛签到失败"
          fi

          # 还原换行符并构造内容（关键：转义特殊字符）
          CONTENT="### $TITLE\n$(echo "$MSG" | sed 's/\\n/\n/g')\n\n📢 GitHub Actions 自动推送"
          
          # 正确构造JSON（避免URL格式错误）
          JSON_DATA=$(jq -n \
              --arg msgtype "markdown" \
              --arg content "$CONTENT" \
              '{"msgtype": $msgtype, "markdown": {"content": $content}}')
          
          # 发送请求（严格校验URL）
          curl -X POST "$WECHAT_WEBHOOK" \
              -H "Content-Type: application/json" \
              -d "$JSON_DATA" \
              --max-time 10 \
              --retry 2 \
              --silent --show-error  # 静默模式，仅显示错误
