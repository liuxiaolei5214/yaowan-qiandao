name: 药丸论坛签到

on:
  schedule:
    - cron: '30 2 * * *'  # UTC 2点 = 北京时间10点
  workflow_dispatch:

jobs:
  checkin:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install requests cryptography

      - name: 记录触发时间
        run: |
          echo "定时任务触发成功，当前时间（UTC）：$(date -u)"
          echo "当前时间（北京时间）：$(date +'%Y-%m-%d %H:%M:%S' -d '+8 hours')"

      - name: 执行签到脚本
        id: checkin_step
        env:
          INVITES_COOKIE: ${{ secrets.INVITES_COOKIE }}
          INVITES_USERNAME: ${{ secrets.INVITES_USERNAME }}
          INVITES_PASSWORD: ${{ secrets.INVITES_PASSWORD }}
        run: python checkin.py

      # 替换为稳定的企业微信通知插件（解决参数错误核心问题）
      - name: 发送企业微信通知
        uses: appleboy/wechat-work-action@master
        with:
          # 该插件标准参数：webhook + 消息类型 + 内容
          webhook: ${{ secrets.WECOM_WEBHOOK }}
          msgtype: markdown
          content: |
            ### 药丸论坛签到通知
            > **状态**：${{ steps.checkin_step.outputs.checkin_result == 'success' && '✅ 签到成功' || '❌ 签到失败' }}
            > **详情**：${{ steps.checkin_step.outputs.checkin_msg || '暂无详细信息' }}
            > **执行时间**：${{ github.run_at || '未知' }}
            > **🤖 GitHub Actions 自动推送**
        continue-on-error: true
