name: 药丸论坛签到

on:
  schedule:
    - cron: '0 1 * * *'  # UTC 1点 = 北京时间9点
  workflow_dispatch:

jobs:
  checkin:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    outputs:
      checkin_result:  $ {{ steps.checkin_step.outputs.checkin_result }}
      checkin_msg:  $ {{ steps.checkin_step.outputs.checkin_msg }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: 安装依赖（含加密库）
        run: |
          python -m pip install --upgrade pip
          pip install requests cryptography
      
      - name: 记录触发时间
        run: |
          echo "定时任务触发成功，当前时间（UTC）： $ (date -u)"
          echo "当前时间（北京时间）： $ (date +'%Y-%m-%d %H:%M:%S' -d '+8 hours')"
      
      - name: 执行签到脚本
        id: checkin_step
        env:
          INVITES_COOKIE:  $ {{ secrets.INVITES_COOKIE }}
          INVITES_USERNAME:  $ {{ secrets.INVITES_USERNAME }}
          INVITES_PASSWORD:  $ {{ secrets.INVITES_PASSWORD }}
          GITHUB_TOKEN:  $ {{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY:  $ {{ github.repository }}
        run: python checkin.py
      
      - name: 发送钉钉通知
        if:  {{ secrets.WECOM_WEBHOOK != '' }}
        uses: zcong1993/dingtalk-action@v2
        with:
          url:  $ {{ secrets.DINGTALK_WEBHOOK }}
          msg: |
            # 药丸论坛签到通知
            ##  $ {{ steps.checkin_step.outputs.checkin_result == 'success' && '✅ 签到成功' || '❌ 签到失败' }}
            ---
             $ {{ steps.checkin_step.outputs.checkin_msg }}
            ---
            🤖 来自 GitHub Actions 自动推送
          type: markdown
        continue-on-error: true
      
      - name: 发送企业微信通知
        if:  {{ secrets.WECOM_WEBHOOK != '' }}
        uses: chf007/action-wechat-work@master
        with:
          msgtype: markdown
          webhook:  $ {{ secrets.WECOM_WEBHOOK }}
          content: |
            ### 药丸论坛签到通知
            > **状态**： $ {{ steps.checkin_step.outputs.checkin_result == 'success' && '✅ 签到成功' || '❌ 签到失败' }}
            > **详情**：
            >  $ {{ steps.checkin_step.outputs.checkin_msg }}
            > **执行时间**： $ {{ github.run_at }}
            > **🤖 来自 GitHub Actions 自动推送**
        continue-on-error: true
      
      - name: 发送 Server 酱通知
        if:  {{ secrets.SERVERCHAN_SENDKEY != '' }}
        run: |
          curl -X POST "https://sctapi.ftqq.com/ $ {{ secrets.SERVERCHAN_SENDKEY }}.send" \
          -d "title=药丸论坛签到- $ {{ steps.checkin_step.outputs.checkin_result }}" \
          -d "desp=### 药丸论坛签到详情\n\n $ {{ steps.checkin_step.outputs.checkin_msg }}\n\n---\n🤖 来自 GitHub Actions 自动推送"
        continue-on-error: true
      
      - name: 发送签到结果邮件
        if:  {{ secrets.EMAIL_USERNAME != '' && secrets.EMAIL_PASSWORD != '' && secrets.NOTIFY_EMAIL != '' }}
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.qq.com
          server_port: 465
          username:  $ {{ secrets.EMAIL_USERNAME }}
          password:  $ {{ secrets.EMAIL_PASSWORD }}
          subject: 药丸论坛签到通知 -  $ {{ steps.checkin_step.outputs.checkin_result }}
          body: |
            【药丸论坛签到任务执行结果】
            ————————————————————
             $ {{ steps.checkin_step.outputs.checkin_msg }}
            ————————————————————
            邮件由 GitHub Actions 自动发送
          to:  $ {{ secrets.NOTIFY_EMAIL }}
          from: GitHub Actions
          secure: true
        continue-on-error: true
